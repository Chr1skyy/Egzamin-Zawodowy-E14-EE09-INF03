1. SELECT imie, nazwisko FROM Kelnerzy ORDER BY nazwisko DESC;
2. SELECT COUNT(*) FROM Zamowienia JOIN Stoliki ON Stoliki.id = idStolika WHERE numerStolika = 3 AND sala = 1;
3. SELECT nazwa FROM Potrawy JOIN ZamowieniaPotrawy ON idPotrawy = Potrawy.id JOIN Zamowienia ON idZamowienia = Zamowienia.id WHERE DATE(dataZlozenia) = '2024-06-09';
4. CREATE USER administator@localhost IDENTIFIED BY 'boss123!'
5. ALTER TABLE Potrawy MODIFY cena DECIMAL(6,2) NOT NULL;