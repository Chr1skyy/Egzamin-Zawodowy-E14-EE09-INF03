1. SELECT imie, kolor, cena FROM klienci WHERE topKlient = 1;
2. SELECT imie, cena FROM klienci WHERE termin = '2024-05-23';
3. SELECT klienci.imie, klienci.kolor, ksztalty.ksztalt FROM klienci JOIN ksztalty ON klienci.ksztalty_id = ksztalty.id WHERE ksztalty.ksztalt = 'migdał';
4. ALTER TABLE wzory ADD COLUMN doplata INT;