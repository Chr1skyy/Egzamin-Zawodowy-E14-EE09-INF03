1. CREATE USER 'Agnieszka' IDENTIFIED BY 'haslo4';
2. SELECT AVG(rocznik) FROM samochody WHERE marka = 'opel';
3. SELECT samochody.marka, klienci.imie FROM samochody JOIN wypozyczenia ON samochody.id = wypozyczenia.samochody_id JOIN klienci ON wypozyczenia.klienci_id = klienci.id;
4. ALTER TABLE klasa DROP COLUMN cena;